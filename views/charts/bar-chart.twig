{% set uniqueID = random() %}

{#<pre>{{dump(layout.line_chart)}}</pre>#}
{% set current_chart = layout.bar_chart %}
{% set c = current_chart|json_encode() %}

<script>
jQuery(document).ready(function($) {
	$(window).load(function() {
		getBarChart(
					'{{ c }}',
					'{{ uniqueID }}',
					'{{ chart_type }}'
				);
	});
});
</script>

<canvas id="barchart_{{ uniqueID }}"></canvas>