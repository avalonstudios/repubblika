{% set uniqueID = random() %}

{#<pre>{{dump(layout.line_chart)}}</pre>#}
{% set current_chart = layout.line_chart %}
{% set c = current_chart|json_encode() %}

<script>
jQuery(document).ready(function($) {
	$(window).load(function() {
		getLineRadarChart(
				'{{ c }}' ,
				'{{ uniqueID }}',
				'{{ chart_type }}'
				)
	});
});
</script>

<canvas id="lineradarchart_{{ uniqueID }}"></canvas>