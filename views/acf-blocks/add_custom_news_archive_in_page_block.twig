


{#
<div class="col-12">
	<hr class="my-2">
	<div class="ml-4">
		{% for post in posts %}
			{% set link = post.post_link.ID %}
			{% set title = post.post_link.post_title|e('wp_kses_post') %}
			{% if title %}
				<a class="ava-nv" href="{{ link|e('esc_url') }}"><i class="far fa-dot-circle mr-2"></i>{{ title }}</a><br>
			{% else %}
				<a class="ava-nv font-italic" href="{{ link|e('esc_url') }}"><i class="far fa-dot-circle mr-2"></i>(no title)</a><br>
			{% endif %}
		{% endfor %}
	</div>
	<hr class="my-2">
</div> #}